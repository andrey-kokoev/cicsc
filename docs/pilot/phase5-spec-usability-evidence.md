# Phase 5 Spec Usability Evidence

Artifact:
- `docs/pilot/phase5-spec-usability-evidence.json`

This evidence confirms the selected reference vertical (`ticketing`) is authored in
Spec DSL surface (`entities`, commands, reducers, views, slas), then compiles and
typechecks into Core IR without requiring authoring directly in IR `types` shape.

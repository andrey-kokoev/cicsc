# Phase 8 Required Gates

Report artifact:
- `docs/pilot/phase8-required-gates.json`

Mandatory checks:
- equivalence gate (`phase8_large_cardinality_differential.sh` + `phase8_edgecase_parity.sh`)
- resilience gate (`phase8_resilience_slo_gate.sh`)
- ergonomics safety gate (negative + assistant + benchmark + report tests)

This artifact closes `P8.4.2` / `Y4.2`.

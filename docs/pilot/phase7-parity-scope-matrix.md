# Phase 7 Parity Scope Matrix

Scope artifact:
- `docs/pilot/phase7-parity-scope-matrix.json`

Frozen dimensions:
- in-scope and out-of-scope query operators
- in-scope and out-of-scope expression operators
- semantic parity axes: null ordering, collation, numeric behavior

This artifact defines the contract boundary for `P7.1.1` / `X1.1`.

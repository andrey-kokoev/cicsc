# Phase 6 SDK Contract Tests

Contract artifact:
- `docs/pilot/phase6-sdk-contract.json`

Validated contract surfaces:
- Bundle lifecycle (`publishBundle`, `resolveBundle`)
- Tenant binding (`bindTenant`, `getTenantBinding`, `listTenantBindingAudit`)

This artifact is the evidence baseline for `P6.3.2` / `W3.2`.

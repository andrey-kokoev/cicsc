# CICSC Kernel

Kernel exports semantic core components:

- IR typechecker and bundle validator
- Oracle replay and verification
- Migration transform and migration verification

Kernel excludes HTTP transport and SQL adapter concerns.
